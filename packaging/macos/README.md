# macOS

Launcher Docker: [`scripts/macos/iniciar-docker.command`](../../scripts/macos/iniciar-docker.command).

Equivalente en el runner / Makefile (clone con Python):

```bash
make docker-up
make docker-down
```

Instalador `.dmg` nativo: pendiente (post v1.1.0).
