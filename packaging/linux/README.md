# Linux

Launcher Docker: [`scripts/linux/iniciar-docker.sh`](../../scripts/linux/iniciar-docker.sh).

Equivalente en el runner / Makefile (clone con Python):

```bash
make docker-up
make docker-down
```

AppImage / .deb: pendiente (post v1.1.0).
