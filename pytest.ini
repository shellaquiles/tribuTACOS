[pytest]
pythonpath = backend .
testpaths = backend/tests control_panel/tests
filterwarnings =
    ignore::DeprecationWarning
    ignore::UserWarning
markers =
    gui: pruebas que instancian Tkinter (requieren display o xvfb)
